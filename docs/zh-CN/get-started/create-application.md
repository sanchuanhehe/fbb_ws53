# 创建应用

优先从 `src/application/samples/` 中选择最接近需求的案例，复制其组件组织方式并在对应 `CMakeLists.txt` 中声明源文件、公共头文件和编译选项。随后通过 Kconfig 将应用纳入 `ws53_liteos_app` 目标。

组件定义和系统能力请继续阅读 [SDK 开发](../guides/sdk-development/index.md)。
